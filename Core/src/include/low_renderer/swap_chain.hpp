#pragma once

#include "core_header.hpp"

BEGIN_PCCORE

class SwapChain 
{
public:

    PC_CORE_API SwapChain() = default;
    
    PC_CORE_API virtual ~SwapChain() = default; 
private:
    
};

END_PCCORE