#pragma once

/*
#include "GpuBuffer.hpp"

BEGIN_PCCORE
class ShaderStorage : public PC_CORE::GpuBuffer
{
public:

    void Update(void* data, size_t size);
    
    ShaderStorage(void* _data, size_t _size, MemoryUsageType MemoryUsageType);

    ShaderStorage(size_t _size, MemoryUsageType MemoryUsageType);
    
    ~ShaderStorage();
    
private:
    
};

END_PCCORE
*/
